[package]
name = "ch8"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
rand = "0"
sdl2 = { version = "0.38.0", features = ["bundled"] }
log = "0.4"
env_logger = "0.11"

#[features]
#default = ["bundled-sdl"]
#bundled-sdl = []
#system-sdl = []
